import 'package:flutter/material.dart';

class GroupManageView extends StatelessWidget {
  const GroupManageView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
